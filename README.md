### 应用于小学自动生成口算算式的工具类

---

##### 使用方法
```
// 返回node节点
var node = $Z_KS.make();
// 获取node中各个位置的参数
/**
 * [1, '+', 1, '+', 3]
 */
node.getNode();
// 得到最终的结果 : 5
node.getValue();
// 大家可以根据自己的业务逻辑将node组合成自己想要展现的形式, 如 1+_+3=5,作为学生的填空题
```
##### 参数说明
```
maxLen: 2, // 算式中数字的最大位数, 仅 + - 并且num=2时有效
minLen: 1, // 算式中数字的最小位数
num: 4, // 左侧数字的个数
needBracket: true, // 是否需要括号影响算式计算顺序，开启后是随机产生括号的
jinwei: true, // 加法是否开启进位
tuiwei: true, // 减法是否开启退位
symbol: parseInt(1111, 2), // 1 加 10->2 减 100->4 乘 1000->8 除, 2进制表示要组合的加减乘除算法
tryTime: 10 // 生成算式失败，重复尝试最大次数
```

### 喜欢的话请点个star支持一下